export default function CDSDomainIPPage() {
  return (
    <div className="rounded-xl border border-base-600 bg-base-900 p-6 shadow-glow text-left">
      <h2 className="text-xl font-semibold text-ink-100 font-display">Domain & IP</h2>
      <p className="mt-2 text-sm text-ink-400 font-mono">หน้าจัดการข้อมูล Domain & IP</p>
    </div>
  );
}
